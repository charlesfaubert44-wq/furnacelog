# Audit security automatically on install
audit=true

# Only allow known registries
registry=https://registry.npmjs.org/

# Require exact versions in production
save-exact=true

# Automatically update lockfile
package-lock=true
